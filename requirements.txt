# Pinned to the versions used to produce the results in this repo.
# NumPy must be <2 for torch 2.2.x interop; xgboost pinned to avoid GPU/NCCL deps.
numpy==1.26.4
pandas>=2.0
scipy>=1.10
scikit-learn>=1.3
torch==2.2.2
xgboost==2.0.3
shap==0.49.1
matplotlib>=3.7
ucimlrepo>=0.0.7
xlrd>=2.0.1          # reading the Credit Card .xls
# Optional, for running the notebooks:
# jupyter, ipykernel
